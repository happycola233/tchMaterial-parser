这是一个面向 [国家中小学智慧教育平台](https://basic.smartedu.cn/) 的桌面工具，用来解析并批量下载电子课本。

深度思考，积极搜索互联网查阅最新开发文档与行业最佳实践。
确保代码可维护性，确保跨平台兼容性，避免回归，拒绝代码冗余与「屎山」堆积。
命名及时重构，新变量命名需语义清晰，同时更新相关的旧变量名。
代码意图不明确时，主动编写清晰准确的中文注释。
涉及到 UI 的调整需同时兼顾浅色与深色模式。
需要任何工具或依赖，请自行安装。
临时文件统一放在 `.tmp/` 目录（该目录已加入 `.gitignore`，不存在时自行创建）。
未被 `.gitignore` 忽略的文件不得包含敏感数据。

不要为那些不可能发生的场景添加错误处理、回退逻辑或校验。应当信任内部代码以及框架提供的保证。只在系统边界处进行校验，例如用户输入、外部 API 等，防止过度工程化编程。
UI 界面上所有文案必须是站在使用者角度、可直接交付的最终产品语言，不得出现描述需求/规则/适用条件本身的元语言。

被要求写 commit 时，只撰写 conventional commit 文案，不要执行 commit 和 push（除非用户提到），
内容应当描述最后一个「最后一个 commit → 当前工作区」的整体 diff，
而不应该提及本次实现过程中的尝试、报错、排查、返工或中间修正的迭代过程。

默认无需阅读 `.devfiles/` 下的文件（除非提到），且禁止做任何修改。